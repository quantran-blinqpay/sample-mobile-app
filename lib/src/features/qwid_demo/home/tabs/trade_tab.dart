import 'package:flutter/material.dart';

class TradeTab extends StatelessWidget {
  const TradeTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Trade Tab'),
    );
  }
}